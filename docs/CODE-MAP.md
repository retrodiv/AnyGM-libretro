<!-- SPDX-License-Identifier: MIT -->
<!-- Copyright (c) 2026 retrodiv <retrodiv@proton.me> -->

# Code map

This map is optimized for quick navigation by maintainers.
Start with the responsibility row, inspect the named public header, and keep a
change inside that owner unless an architectural seam genuinely changes.

| Path | Responsibility | Public or internal entry points | Mutable owner | Allowed dependencies | Forbidden dependencies | Compatibility policies | Tests | Focused command |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `src/content/container/content_pipeline.c` | Explicit bounded native buffer operations for ordered input pipelines | `content_pipeline.h`; `docs/CONTENT_TRANSFORMS.md` | One caller-owned result per step | Standard C and the shared policy-free buffer inflater | Source probing, host callbacks, files, network, native modules or bundled recovery programs | None | Wrapper/inflate/edit composition, endian, capacity, integrity and transactional rejection | `make check TEST=content_transform` |
| `src/content/container/content_delta.c` | Bounded in-memory VCDIFF decoder and sole raw xdelta interface owner | `content_delta.h`; explicit source, patch, result size and workspace budget | One decode transaction and shared auxiliary allocation budget | Vendored decoder-only xdelta3 and private liblzma closure | Host callbacks, files, subprocesses, networking, identity policy and vendor types in callers | None; owned output is published only on complete success | Authored COPY/ADD, secondary compression, truncation, corruption and resource-limit cases | `make check TEST=content_delta` |
| `src/content/container/content_source.c` | Bounded configured source-range enumeration, independent normalization and unique structural selection | `content_source.h`; `docs/CONTENT_TRANSFORMS.md` | One immutable source and one candidate-selection transaction | Transform registry and caller-supplied structural validator | Format signatures, host I/O, native modules, reader implementations or runtime publication | Explicit input declarations only | Range table validation, ambiguity, rejection, identity and isolated candidates | `make check TEST=content_transform`; `make check TEST=content_security` |
| `src/content/container/content_config.c` | Bounded system INI sections, SHA-256 selectors and transactional source validation | `content_config.h`; `docs/CONTENT_CONFIGURATION.md` | One immutable parsed configuration per resolution | Standard C and bounded transform compiler/interpreter | Host I/O, runtime override semantics, native code or bundled content identities | External declarations only | Section parsing, exact selection, source isolation and rejection; router and engine integration | `make check TEST=content_config`; `make check TEST=content_security`; `make check TEST=engine_content_config` |
| `src/content/container/content_transform.c` | Bounded user-supplied buffer programs and transactional configuration | `content_transform.h`; `docs/CONTENT_TRANSFORMS.md` | One program set, private execution buffers and bounded read-only caller metadata | Standard C and policy-free SHA-256 | Host callbacks, files, network, native execution, bundled format programs or keys | None | Synthetic arithmetic, isolation, metadata propagation, bounds, and override cases | `make check TEST=content_transform` |
| `src/content/container/content_transform_source.c` | Bounded C-like transform lexer and compiler into the existing buffer instruction set | `content_transform_source.h`; configuration and anchor source consumers | One compile transaction, scoped locals and temporary registers | Standard C and the transform instruction definitions | Runtime VM, host capabilities, native code, bundled format programs or keys | No persistent state; compiled program identity remains with the configuration owner | Synthetic syntax, scope, control-flow, limit, diagnostic and malformed-source cases | `make check TEST=content_transform`; `make check TEST=content_security` |
| `src/api/` | Framework-neutral public C ABI | `anygm.h`; `AnygmEngine`; `AnygmHostServices`; prepared-content facts | Opaque `AnygmEngine`; no public global state | C fixed-width and size headers | Internals, adapters, platform headers | Neutral facts and profile are opaque | `tests/contract/public_header_*` | `make api-check` |
| `src/core/` | Prepared/start lifecycle, frame orchestration, transactional language-triggered content replacement, configuration, and state root | `anygm_create`; `anygm_load_prepare`; `anygm_load_start`; `anygm_load`; `anygm_run_frame`; state API | `AnygmEngine` and its owned subsystem contexts | API, host, compatibility, content, runtime, video, audio | Adapter headers and ambient OS services | Consumes the immutable resolved profile | `tests/contract/dummy_host.c`; neutral content-replacement and presentation cases in `tests/integration/test_engine_instances.c`; state corpus | `make integration-check`; `make contract-check` |
| `src/compatibility/` | Structural facts and immutable semantic policy | `anygm_content_facts_detect`; `anygm_compatibility_resolve`; policy accessors | Engine-owned immutable `AnygmCompatibilityProfile` | Neutral content structures and standard C | Adapters, paths, content identity, host globals | Sole owner of cross-generation semantic selection | `tests/unit/compatibility/test_compatibility.c`; architecture scan | `make check TEST=compatibility` |
| `src/content/container/` | Content routing, safe archive extraction, reported asset root, paths, and disposable cache | `anygm_content_resolve_path`; `anygm_content_load_win` | One route/extraction transaction; cache is disposable | Host VFS and concrete normalizers | Shell, process execution, plugins, network, adapters | Detect structure only; does not select runtime semantics | `tests/fuzz/test_content_security.c` | `make check TEST=content_security` |
| `src/content/container/embedded_cab.c` | Bounded PE-section Cabinet discovery, profile classification, bounded in-memory CAB parse and LZX-21 decompression, safe transactional extraction, and complete cache verification | `anygm_embedded_cab_probe`; `anygm_embedded_cab_extract` | One bounded source handle, extraction manifest, and staging namespace | Host VFS and an unresolved mspack decompressor interface in this historical revision | Whole-source copies, filename-opening archive helpers, native execution, platform Cabinet APIs, or ambient filesystem fallback | Detects packaging only; supported payloads rejoin the ordinary Studio loader | Neutral split-record LZX fixture, memory VFS, malformed corpus, cache and fault cases in `tests/fuzz/test_content_security.c` | `make check TEST=content_security` |
| `src/content/container/embedded_nsis.c` | Bounded NSIS 2 ANSI non-solid Deflate recognition and safe transactional extraction | `anygm_embedded_nsis_probe`; `anygm_embedded_nsis_extract` | One source image, extraction plan, and staging namespace | Host VFS and the shared policy-free inflate seam | NSIS/7-Zip code, external archivers, native execution, platform installer APIs, or ambient filesystem fallback | Detects packaging only; supported payloads rejoin the ordinary Studio loader | Neutral stored-block, traversal, unsupported-profile, and direct-route cases in `tests/fuzz/test_content_security.c` | `make architecture-check`; `make check TEST=content_security` |
| `src/content/container/cab_lzx.c` | First-party LZX-21 decompressor for the single-folder embedded Cabinet profile, written from the public bitstream format | `cab_lzx21_decompress`, used only by `embedded_cab.c` | One caller-owned input and output buffer | Standard C only | Dynamic allocation, external libraries, other compression methods or windows, and any read or write outside its two buffers | None | Synthetic malformed-content security fixture; external differential validation is not recorded here | `make architecture-check`; `make check TEST=content_security` |
| `src/content/datafile/` | Bounded normalized FORM decoding and indexes | `gml_win_from_mem`; `gml_win_load_host`; `GmlWin` queries | `GmlWin` owns input, tables, indexes, and decode caches | Host VFS and bytecode layout readers | Adapters, direct filesystem, runtime behavior | Exposes structural facts; no family-wide semantic branching | `tests/fuzz/test_datafile_security.c` | `make check TEST=datafile_security` |
| `src/content/bytecode/` | Encoding-specific instruction and reference-layout readers | `gml_decode_bc_bounded`; `gml_bc_code_start`; `gml_bc_ref_layout` | No independent state; caches belong to `GmlWin` | Datafile model and standard C | Runtime scheduling, adapters, host services | Encoding dispatch only; per-record layout detection where required | `tests/unit/content/test_bytecode.c`; `tests/fuzz/test_bytecode_security.c` | `make check TEST=bytecode_security` |
| `src/content/project/` | Source-project parsing, full-length string-literal compilation, and normalized package generation | `gmlc_project_load`; `gmlc_bytecode_compile_source_ex`; `gmlc_package_write_structural` | `GmlcProject`, compiler registries, and package transaction | Host VFS, JSON, bytecode model, image writers | Adapters and ambient process configuration | Produces explicit structural metadata; no runtime policy | `tests/unit/content/test_bytecode.c`; `tests/unit/content/test_package.c`; normalized-data corpus | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/classic/` | Normalized Classic readers, optional context-bearing record preparation, hash-bound exact-fidelity restoration, and extension normalization into the shared package | `gmlc_classic_prepare_record`; fidelity, import, and project entry points | `GmlcClassicManifest` and one import transaction | Host VFS, project model, bounded buffer pipelines, SHA-256, image/font helpers | Adapters, runtime execution, ambient filesystem fallback, bundled record operations, or game identities | Publishes structural record kind/revision and external-call aliases, never an algorithm selector | Named resource-family, optional record, extension, fidelity, and malformed-input cases under `tests/unit/content/classic/` | `make check TEST=classic` |
| `src/media/` | Policy-free SHA-256, static/animated image, inflate, and TrueType raster helpers | `gml_hash.h`; `gml_image_codec.h`; `gml_font_raster.h`; fixed digest and owned media results | Per-call hash/image buffers and bounded opaque font lifetimes | Vendored stb headers and standard C allocation | Paths, host services, atlas layout, runtime policy, game identities, or third-party types in callers | None; callers own format interpretation, matching policy, output limits, animation policy, and glyph placement | independent NIST SHA-256 vectors; `tests/unit/media/test_image_codec.c`; `tests/unit/media/test_font_raster.c`; content and renderer owner suites | `make check TEST=hash`; `make check TEST=image_codec`; `make check TEST=font_raster` |
| `src/runtime/vm/` | Shared values, bytecode execution, instances, events, rooms, draw scheduling, collision policy, and state | `gml_value.h`; `gml_vm.h`; VM state API | `GmlVM` and its owned value graphs, particle and software-3D contexts; builtin storage is opaque | Compatibility accessors, builtin operations, renderer operations and metadata snapshots, owned AV contexts, host callbacks | Builtin-resource layout or field dereferences, renderer-private records or fields, adapters, raw version predicates, ambient OS state | Consumes named VM, event, collision, path, and RNG policies | runtime unit suites; builtin-state lifetime; engine isolation | `make check TEST=persistent_room`; `make check TEST=builtin_state` |
| `src/runtime/builtins/` | Canonical exact-name metadata and resolution, direct ID/stage execution, ordered contextual fallback, family argument adaptation, bounded classic dynamic-call and portable external-audio adaptation, language-visible host services, and builtin-resource lifetime | `gml_builtin_registry.h`; `gml_builtin_call`; encoded external-call slow path; ordered `gml_builtin_try_*` stages; builtin-state operations | One `GmlBuiltinState` owns INI, file/buffer, async, DS, time-source, spatial/external-audio/Saudio string-ID, physics, and dispatcher pools; borrows other VM-owned subsystem contexts | VM, compatibility accessors, host/VFS, typed AV operations and value snapshots; opaque VM-state cursor only in the state owner | Duplicate exact-name tables or resource owners, video records or fields, backend-only raster leaves, adapters, raw version predicates, direct OS APIs, runtime module loading, game-specific hashes, or per-call function-pointer dispatch | Consumes named builtin, draw, file, input, and platform policies; cache policy preserves contextual and script precedence | exhaustive registry consistency, encoded-call dispatch, external-audio playback/state, persistent-room, D3-state, and data-structure suites | `make builtin-registry-check`; `make check TEST=builtin_dispatch`; `make check TEST=builtin_state`; `make check TEST=persistent_room` |
| `src/runtime/particles/` | Engine-owned particle simulation, shape sampling, draw orchestration, and serialization | particle create/update/draw/state entry points | `GmlParticleSystem` owned by one VM | VM, renderer operations and non-reentrant neutral draw views, compatibility accessors | Renderer-private records or fields, adapters, global mutable registries, direct OS APIs | Consumes named draw and simulation policies | Named particle and raster cases under `tests/unit/video/software3d/` | `make check TEST=d3_state D3_TEST_ARGS='--case raster.particles'` |
| `src/video/renderer/` | Software rendering, assets, surfaces, fonts, and render state | `gml_render_init`; draw entry points; coarse control, resource, diagnostic, draw, target, and presentation snapshots or masked updates | `GmlRender` and its worker/cache members | Compatibility accessors, host/VFS, codecs, immutable assets | Adapters, raw version predicates, ambient graphics APIs | Consumes named draw, blend, collision-geometry, and text policies | D3-state and persistent-room suites | `make check TEST=d3_state` |
| `src/video/software3d/` | Single software fixed-function, vertex/model, and 3D raster pipeline | `gml_software3d.h`; typed state, model, and draw operations | One `GmlSoftware3D` context per engine | Renderer non-reentrant backend services and standard C | Builtin implementation, renderer facade recursion, adapters, ambient graphics APIs | Consumes normalized draw policy supplied by callers | D3 state, raster, and model fixtures | `make check TEST=d3_state` |
| `src/video/gpu/` | Optional hardware execution of eligible final plans and unrecognized content GLSL: context lifetime, capability and resource generations, and one direct OpenGL / OpenGL ES backend | `gml_gpu.h`; create, context reset, context loss, destroy, plan execution, counters | One `GmlGpu` owned by an `AnygmEngine`; every handle and dispatch pointer is instance-owned | The neutral render plan, the host-supplied entry-point callback, and standard C | Renderer or engine internals, frontend-adapter types, platform graphics libraries, window or device APIs, ambient symbol lookup, process-global dispatch, or content identity | None; eligibility is a structural fact about the pass | Named lifecycle and execution cases driven by a graphics driver written in the test | `make check TEST=gpu` |
| `src/video/assets/` | Independently generated visual fallback assets | `gm_qoi.h` immutable data | None | Renderer only | Runtime mutation and adapter dependencies | None | renderer fixtures and provenance review | `make check TEST=d3_state` |
| `src/audio/mixer/` | Voices, audio groups, SHA-identified bounded dynamic WAV/OGG/MP3 registration and exact-handle rehydration, gain envelopes, mixing, and audio state | `gml_audio_init`; dynamic-sound, content-hash, playback, mix, and state entry points | `GmlAudio` | Host/VFS, policy-free SHA-256, banks, codecs | Adapters, direct audio devices, raw version predicates | Consumes named audio behavior policies | persistent-room and state corpora | `make check TEST=persistent_room` |
| `src/audio/codecs/` | Bounded compressed-audio decoder wrappers | codec open/decode/free entry points | Decoder instance owned by `GmlAudio` | Vendored decoder headers | Host devices, adapters, runtime semantics | None | audio fixtures in persistent-room suite | `make check TEST=persistent_room` |
| `src/audio/banks/` | Audio-bank metadata, stream lookup, bounded FSB5 header parsing, and in-memory Wwise Vorbis reconstruction | `gml_fmod_bank_open`; lookup/decode helpers; `gml_wwise_wem_to_ogg` | Bank objects remain owned by their runtime callers | Host/VFS, codec helpers, immutable setup/codebook data, vendored bounded reconstruction code | Adapters and direct filesystem | Structural layout selection only | Audio fixtures in persistent-room suite; synthetic FSB5 direct and bank-boundary corpus | `make check TEST=persistent_room`; `make check TEST=fmod_security` |
| `src/host/` | Portable helpers over `AnygmHostServices`; explicit test stdio VFS; aarch64-linux cross-toolchain glibc symbol-version compatibility; embedded notice text | `anygm_host_*`; `anygm_vfs_*`; `anygm_stdio_vfs_services_init`; `anygm_third_party_notices`; linker-interposed `__wrap_sqrtf`/`__wrap_atan2f` (aarch64-linux builds only, see `anygm_libm_compat.c`) | Host-owned callback handles; helpers retain no process-global state | Public API and standard C; OS calls only in test stdio backend | Adapters in portable helpers and implicit filesystem fallback | None | `tests/unit/host/test_vfs.c`; dummy host | `make check TEST=vfs`; `make notices-check` |
| `src/adapters/libretro/` | Libretro callbacks, two-phase content startup, independent graphics-option policy, input mapping, frontend VFS, and A/V transport | Standard `retro_*` exports; `LibretroAdapter` | One frontend-owned `LibretroAdapter` | Public AnyGM API and vendored libretro header | AnyGM internals and semantic/version decisions | None; maps neutral API values only | option, hardware-context, export, and state-transport contracts | `make contract-check`; `make export-check` |
| `src/generated/` | Checked-in immutable data and deterministic lookup indexes with documented provenance | Generated data headers | None | Owning renderer, audio, builtin-registry, content, or host module | Runtime mutation, adapters, undocumented regeneration, or hand-edited derived indexes | None | owning subsystem fixtures, generator drift checks, and provenance review | `make builtin-registry-check`; `make notices-check`; `make check TEST=d3_state` |
| `src/third_party/` | Vendored dependencies with preserved upstream notices | Upstream APIs wrapped by owning modules | Library context owned by caller | Owning wrapper only | Product policy, adapters unless vendored for that adapter | None | core link plus notice/license audit | `make core` |
| `tests/unit/` | Narrow parser and semantic characterization | `test_*` executables | Per-test local fixture | Public/internal owner under test and test support | External corpus and network | Exercises both sides of named policies | Unit executables | `make check TEST=compatibility` |
| `tests/unit/content/classic/` | Named synthetic characterization mirrored by classic format and resource-family owner | `classic_test_*_group`; bounded `classic_test_fixture.*` operations | Per-case project, manifest, and bounded fixture values | Classic content APIs, shared media codecs, test runner, explicit test stdio backend, and standard C | External corpus, production linkage, hidden fixture setup, or cross-family assertion ownership | Exercises structural revisions and normalized output without selecting an engine implementation or requiring source/record programs | Filterable named cases, including optional record context and empty text | `make check TEST=classic`; `make check TEST=classic CLASSIC_TEST_ARGS='--case classic.images'` |
| `tests/unit/video/software3d/` | Named software-3D boundary, asset, language/font, particle, projection, surface, blend/shader, primitive/model, and canonical-state cases | `software3d_test_fixture.h`; explicit `AnygmTestCase` table | One clean raster fixture per selected case; bounded prerequisite stages are replayed explicitly | Software-3D, renderer, VM/builtin, particle, VFS, and minimal test runner boundaries | External content, hidden fixture setup, mutable state shared between registered cases, or alternate raster implementations | Exercises shared runtime behavior through synthetic state and exact pixel observables | Twelve filterable named cases | `make check TEST=d3_state`; `make check TEST=d3_state D3_TEST_ARGS='--case raster.projection'` |
| `tests/unit/runtime/persistent/` | Named VM lifecycle, field, sequence, DS, I/O, audio, timeline, state, and renderer cases | `persistent_test_fixture.h`; explicit `AnygmTestCase` table | Per-case VM, package, sandbox, renderer, or audio fixture | Owning runtime interfaces, explicit test VFS, project package writer, and minimal test runner | External content, hidden fixture setup, cross-family dumping-ground cases, or state inherited from another case | Exercises shared classic/Studio semantics and canonical current-schema state without selecting another runtime; `test_persistent_vm.c` deliberately keeps the one ordered lifecycle scenario cohesive because event order is its observable, while `test_persistent_vm_fields.c` owns independent instance-field semantics and `test_persistent_sequences.c` owns bounded sequence parsing and evaluation | Fifty-eight filterable named cases | `make check TEST=persistent_room`; `make check TEST=persistent_room PERSISTENT_TEST_ARGS='--case vm.sequence_asset_keys_and_parameters'` |
| `tests/contract/` | Public API and binary-export contracts | `dummy_host.c`; public-header probes; export checker | Dummy host fixture | Public API, synthetic support, built artifact inspection | Private runtime headers in public-header probes | Neutral host contract only | Contract executables and shell audit | `make contract-check` |
| `tests/integration/` | Cross-module ownership, determinism, and isolation | `test_engine_instances.c` | Two independent test engines | Public API and synthetic content | External corpus and adapter internals | Same resolved profile in independent engines | Engine-instance executable | `make integration-check` |
| `tests/fuzz/` | Deterministic corrupt-input and boundary corpora | Security test executables | Per-case temporary buffers and fixtures | Smallest owning parser/API surface | Network, external corpus, nondeterministic seeds | Invalid input cannot alter selected policy or live state | Content, datafile, bytecode, and state corpora | `make security-check` |
| `tests/architecture/` | Dependency, ownership, source-group, serializer, cleanup-safety, and isolation enforcement | `check_*.sh`; `safe_clean.sh` | None | Source tree and build metadata | Runtime mutation and external services | Enforces named-policy and adapter boundaries | Shell checks | `make architecture-check` |
| `tests/support/` | Shared synthetic content, host fixtures, one fake graphics driver, and minimal named-case coordination | `synthetic_content.*`; `anygm_test_runner.*`; `graphics_driver_fixture.*`; test VFS helpers | Fixture object or test process | Public API, the explicit test-only stdio backend, and the graphics backend's private declaration set | Production linkage, hidden fixture setup, general framework policy, and external corpus | Neutral synthetic content only | Consumed by unit, contract, integration, and security tests | `make check TEST=classic`; `make check TEST=gpu`; `make integration-check` |

## Translation-unit ownership

This compact table refines the directory rows for source groups that have
multiple independently maintained owners. `Makefile.common` is the canonical
source-group inventory.

| File/group | Owns | May call | Must not call | Mutable state | State section | Focused test |
| --- | --- | --- | --- | --- | --- | --- |
| `ANYGM_CORE_SOURCES` | Framework-neutral lifecycle, frame coordination, and root persistence | Host, compatibility, content, VM, renderer, audio | Adapter APIs or ambient OS services | One caller-owned `AnygmEngine` | Root framing only | `make integration-check contract-check` |
| `src/core/engine.c` | Public prepared/start lifecycle entry points and per-frame coordination | Narrow core owners, phase-local renderer packets, bounded application-surface composition views, and subsystem APIs | Subsystem-private algorithms or renderer storage | Coordinates fields stored in `AnygmEngine` | Calls the root state owner | `make integration-check contract-check` |
| `src/core/engine_input.c` | Normalized keyboard, pad, pointer, edge, rumble, and VM input-service bridge | Host input services, compatibility policy, presentation coordinate queries | Adapters or direct platform input APIs | Input snapshots stored in `AnygmEngine` | Input fields are framed by the root owner | `make integration-check contract-check` |
| `src/core/engine_overrides.c` | Runtime-override parsing and application, menu declarations, and explicit room/intro skip hooks | VM operations, host development settings, presentation policy queries | Adapters, content identity, or alternate runtime implementations | Override, menu, and intro state stored in `AnygmEngine` | Override fields are framed by the root owner | `make contract-check TEST=state_security` |
| `src/core/engine_presentation.c` | View selection, aspect policy, software cursor, transitions, GUI composition, and final framebuffer geometry | Renderer APIs, compatibility policy, runtime override queries | Adapters or host video callbacks | Presentation fields stored in `AnygmEngine` | Presentation fields are framed by the root owner | `make integration-check contract-check` |
| `src/core/engine_state.c` | Root header, identity, checksum, canonical section order, transactional restore | Engine diagnostics and subsystem state APIs | Adapters or alternate schema readers | State-restore transaction and reapply buffer | Root state schema | `make integration-check security-check` |
| `src/core/engine_internal.h` | Private engine storage composition and narrow core interfaces | Public API and subsystem context types | Implementation bodies or format-specific engines | Sole definition of `struct AnygmEngine` | Declares state owner entry points | `make architecture-check` |
| `ANYGM_MEDIA_SOURCES` | Path-free implementation owners for shared image decode, deterministic PNG encode-to-memory, zlib/raw-DEFLATE inflate, and TrueType raster primitives | Vendored codec/font implementation headers and standard C allocation | Host services, filesystem paths, content semantics, atlas layout, renderer records, or adapters | Returns standard-heap byte ranges and bounded opaque font-face lifetimes | None | `make check TEST=font_raster` |
| `src/content/project/gmlc_bytecode_internal.h` | Private source-compiler records and narrow lexer/source-scanner operations shared only while parser, emitter, lexer, and function-registry owners cooperate | Public project and bytecode declarations plus standard size and integer types | Package-writer records, classic-import records, runtime state, adapters, implementation bodies, or helpers with only one owner | Declares one compiler transaction, token cursor, byte buffer, macro table, bounded source span, direct source/token scanning calls, and the one room/script code-layout query shared by parser and registry; owner-local preparation and skip helpers stay private | None; generated bytecode and package bytes are protected by exact output characterization | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/project/gmlc_lexer.c` | Sole source preparation, token advancement, full string-token decoding, structural span/delimiter scanning, and function-shape recognition owner | Compiler-private records, project source reads, and standard character/string/allocation operations | Parser statements, bytecode emission, function-registry construction, package records, runtime code, or ambient filesystem access | Owns no persistent state; `compiler_source_text` and `lx_skip` remain owner-local while ten direct operations cross the private compiler boundary | None; exact compiler and package outputs remain unchanged | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/project/gmlc_function_registry.c` | Sole source-function registry, asset/macro/global binding, room/script code-layout, source-function indexing, and unsupported closure-capture analysis owner | Compiler-private lexer/scanner operations, project source reads, normalized project records, and bounded allocation/string operations | Statement parsing, bytecode instruction emission, package chunk layout, runtime semantics, or ambient filesystem access | Owns the temporary registry allocations returned to the package/compiler coordinator; `NameList` and every capture-analysis helper remain owner-local, while only `source_room_code_count` crosses back to parser code | None; emitted compiler and package bytes remain exact | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/project/gmlc_emitter.c` | Sole byte-buffer growth, scalar write, reference/string-site recording, classic call-name canonicalization, instruction emission, and branch-patching owner | Compiler-private transaction and byte-buffer records, normalized project names, math, and bounded allocation/string operations | Source scanning, statement parsing, function-registry construction, package chunk layout, runtime execution, or ambient filesystem access | Owns no persistent state; buffer/site and name-canonicalization helpers remain local, while twenty instruction operations cross the narrow compiler-private boundary to parser code | None; emitted bytecode fixtures and structural package bytes remain exact | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/project/gmlc_parser.c` | Sole recursive-descent expression/statement parser, bounded parser scan, lvalue lowering, macro collection, parameter-prologue, and source compilation-transaction owner | Compiler-private lexer/emitter operations, normalized project/function records, host development logging, opcode/type constants, and bounded standard-library operations | Public bytecode facade, function-registry construction, package chunk layout, runtime execution, or ambient filesystem fallback | Owns transaction-local `CaseRec` and `LValue` representations and temporary compiler allocations; only `compile_text_internal` crosses to the retained facade | None; malformed-source cases, bytecode fixtures, and structural package bytes remain exact | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/project/gmlc_bytecode.c` | Compact public source-bytecode facade: empty blob emission, source/function compilation entry points, wrapper selection, and blob destruction | Public bytecode/project records, compiler-private source read and compile transaction, and bounded allocation/string/diagnostic operations | Lexer internals, grammar implementation, instruction emission, registry construction, package chunk layout, or runtime execution | Owns returned `GmlcCodeBlob` lifecycle only; delegates parsing through the one compiler-private `compile_text_internal` operation | None; public bytecode fixtures and structural package bytes remain exact | `make check TEST=bytecode`; `make check TEST=package` |
| `src/content/project/gmlc_package_internal.h` | One package-private representation shared only by the coordinator, chunk, code/reference, and texture/atlas owners | Public project/package and compiled-bytecode value records plus standard size and integer types | Classic-import records, compiler transaction internals, runtime state, adapters, implementation bodies, or helpers with only one owner | Declares the exact buffer, string/patch, code-reference, texture-placement, reference-layout, and `Pkg` transaction records without changing field layout | None; generated structural package bytes remain exact | `make check TEST=package`; `make architecture-check` |
| `src/content/project/gmlc_package_textures.c` | Sole deterministic texture-request, atlas-packing, sprite/background/path placement, reference-layout validation, atlas-image construction, and `TPAG`/`TXTR` emission owner | Package-private transaction and writer operations, public project records, injected VFS reads, path-free image codec, and bounded standard-library operations | Generic blob-read ownership, other chunk layout, code/reference planning, coordinator lifetime, ambient filesystem access, runtime semantics, or adapters | Owns the local `TextureRequest`, `PackRect`, `PackPage`, and `RefChunk` records; seven direct operations cross the package-private boundary while every packing and reference-parser helper remains local | None; alpha-boundary, packing, reference-layout, and exact structural-package cases retain established output | `make check TEST=package`; `make architecture-check` |
| `src/content/project/gmlc_package_code.c` | Sole compiled-code planning, code/name-reference and reference-chain finalization, deterministic code-name/index derivation, source compilation and placeholder handling, and `CODE`/`TRIG`/`VARI`/`FUNC` emission owner | Package-private transaction/writer operations, source compiler and normalized project records, host diagnostics, normalized bytecode declarations, and bounded standard-library operations | Generic chunk layout, texture packing, coordinator lifetime, classic import, runtime execution, ambient filesystem access, or adapters | Owns the local `RefOrder`, `CodeEntryPlan`, and `StringSeedEvent` records; eleven dependency-derived operations cross the package-private boundary while all ordering and planning helpers remain local | None; compiled bytecode fixtures and exact structural-package bytes retain established output | `make check TEST=bytecode`; `make check TEST=package`; `make architecture-check` |
| `src/content/project/gmlc_package_chunks.c` | Sole package-buffer/string-table, patch-list, generic chunk-framing, bounded blob-read, shader-materialization, and non-code/non-texture structural-chunk owner | Package-private transaction and code/texture index operations, public project records, injected host/VFS services, normalized data declarations, and bounded standard-library operations | Texture packing, code/reference planning, coordinator lifetime/output policy, classic import, runtime execution, ambient filesystem access, or adapters | Owns all chunk-local helpers, ten immutable shader tables; thirty direct dependency-derived writer operations cross the package-private boundary | None; bounded package cases and exact structural-package bytes retain established output | `make check TEST=package`; `make architecture-check` |
| `src/content/project/gmlc_package.c` | Compact public structural-package transaction coordinator, development-option lookup, output and included-file materialization, and package cleanup | Public package/project declarations, package-private owner operations and transaction records, and injected host/VFS services | Buffer/chunk layout, code/reference algorithms, texture/atlas algorithms, source compiler internals, classic import, runtime semantics, ambient filesystem access, or adapters | Owns exactly one stack-local `Pkg` transaction and seven coordinator operations; delegates every construction stage to its declared owner and publishes bytes only after all stages succeed | None; transaction failure and exact structural-package cases retain established output | `make check TEST=package`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import.c` | Compact shared bounded-reader, inflate, cache-path, and source-materialization service owner for all classic import families | Classic-import-private records, normalized project memory-file insertion, path-free DEFLATE, injected host/VFS services, and bounded standard operations | Resource-family parsing, compiler/package internals, runtime execution, ambient filesystem fallback, or adapters | Owns exactly twelve shared-service operations; eleven cross through the private boundary and `write_source` remains local to source materialization | None; malformed payload and memory/file-backed source cases retain established behavior | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_fidelity.c` | Sole bounded parser, project-SHA-256 matcher, and manifest restoration owner for optional exact-fidelity companions | Classic manifests, immutable project bytes, policy-free SHA-256, BZip2 decode, and bounded allocation | Project writing, game identity policy, runtime execution, ambient files, or adapters | Applies only a companion whose embedded version, generation, byte length, and digest match the exact project; rejects duplicate, oversized, trailing, or inconsistent records | No runtime state; restored manifest bytes feed the ordinary classic import transaction | `make check TEST=classic`; `make check TEST=content_security` |
| `src/content/classic/gmlc_classic_import_internal.h` | Private bounded-reader and action-source records plus the thirteen direct operations shared only by classic importer family owners | Public classic-import and normalized-project declarations plus standard size and integer types | Package/compiler records, runtime state, adapters, implementation bodies, or family-local helper records | Declares exactly one `ImportReader`, one `ImportText`, eleven bounded-reader/source-materialization operations, and the two action-source operations required by later resource owners; it does not define a second importer object | None; malformed classic payloads and focused resource cases retain established behavior | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_code.c` | Sole classic script normalization and action-list-to-source construction owner | Classic-import-private bounded readers and source text, normalized project records, and bounded standard allocation/string/formatting operations | Extension packages, other resource parsing, compiler bytecode emission, runtime execution, ambient filesystem access, or adapters | Owns `ActionCodeState` and every action-source helper; only `text_append` and `import_actions` cross the classic-private boundary to later family owners | None; script and action-list synthetic cases retain established source and project output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_extensions.c` | Sole bounded classic extension-package decode, normalization, dependency hashing, alias discovery, and project insertion owner | Public classic manifest/project records, injected host/VFS services, shared source materialization, and bounded standard allocation/string operations | Other classic resource formats, compiler internals, runtime execution, ambient filesystem fallback, or adapters | Owns its decode table, alias records, size limits, and transactional script rollback; no extension helper crosses the classic-private boundary | None; malformed extension packages and deterministic dependency/alias cases retain established behavior | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_images.c` | Sole classic sprite/background image decode, legacy executable binary-alpha normalization, BGRA normalization, collision-mask construction, PNG materialization, and project insertion owner | Classic-import-private bounded readers and cache paths, normalized project records, path-free image codec, injected VFS services, and bounded standard operations | Font rasterization, sound blobs, package atlases, runtime textures, ambient filesystem fallback, or adapters | Owns all ten image-family operations; no image helper crosses the classic-private boundary | None; sprite/background, alpha, mask, malformed-image, and memory/file-backed cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_fonts.c` | Sole compiled-atlas, host-resolved TrueType, and embedded-fallback classic font normalization and raster materialization owner | Classic-import-private bounded readers/cache paths, normalized project records, injected host/VFS and font services, path-free raster API, embedded default font data, and bounded standard operations | Image/sound resource parsing, renderer text layout, ambient font/filesystem fallback, package chunks, runtime execution, or adapters | Owns four named font records, one local category enum, and all seventeen font-family operations; no font helper crosses the classic-private boundary | None; compiled, resolved, fallback, glyph-range, malformed-font, and memory/file-backed cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_audio.c` | Sole classic sound-blob fallback, binary materialization, and normalized sound insertion owner | Classic-import-private cache paths, normalized project records, injected VFS services, and bounded standard allocation/formatting operations | Audio decode/mixing, image/font parsing, ambient filesystem access, runtime semantics, or adapters | Owns all four audio-import operations; no audio helper crosses the classic-private boundary | None; embedded/missing sound and memory/file-backed cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_paths.c` | Sole classic path point normalization and normalized project insertion owner | Classic-import-private bounded readers, normalized project records, and bounded standard allocation/formatting operations | Runtime path stepping, other classic resources, compiler internals, host services, or adapters | Owns both path-family operations; no path helper crosses the classic-private boundary | None; valid, sparse, and malformed path cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_objects.c` | Sole classic object record, sprite-link, and event/action-source normalization owner | Classic-import-private bounded readers/action source, normalized project records, and bounded standard allocation/formatting operations | Runtime instance/event execution, sprite decoding, timelines/rooms, compiler internals, host services, or adapters | Owns all four object-family operations; consumes the two declared action-source crossings without exposing an object-private helper | None; sparse object, event ordering, action decoding, and malformed-object cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_timelines.c` | Sole classic timeline moment and action-source normalization owner | Classic-import-private bounded readers/action source, normalized project records, and bounded standard allocation/formatting operations | Runtime timeline stepping, objects/rooms, compiler internals, host services, or adapters | Owns both timeline-family operations; consumes the two declared action-source crossings without exposing a timeline-private helper | None; sparse timeline, moment ordering, action decoding, and malformed-timeline cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `src/content/classic/gmlc_classic_import_rooms.c` | Sole classic room, instance/tile placement, creation-code, and room-order normalization owner | Classic-import-private bounded readers/action source, normalized project records, injected host diagnostics, and bounded standard allocation/formatting operations | Runtime room entry/layers, object or image parsing, compiler internals, ambient filesystem access, or adapters | Owns all four room-family operations; consumes the declared action-source crossings and keeps room-code materialization local | None; sparse room, placement/order, creation/action code, and malformed-room cases retain established output | `make check TEST=classic`; `make architecture-check` |
| `ANYGM_CLASSIC_TEST_SOURCES` | Minimal named-case runner, one bounded shared fixture owner, format/container cases, resource-family cases, and the suite coordinator | Classic content APIs, generated private test declarations, explicit test stdio backend, vendored test-only image helpers, and standard C | Production linkage, external corpus, hidden fixture setup, duplicate family assertions, or a general test framework | One process-local host fixture plus per-case bounded fixture, manifest, and project values; each case table remains in its resource-family file | None; test data is synthetic and state blobs are not persisted | `make check TEST=classic`; `make check TEST=classic CLASSIC_TEST_ARGS='--case classic.rooms'` |
| `src/runtime/builtins/gml_builtin_internal.h` | Private family-stage declarations, the sole concrete `GmlBuiltinState` resource layout, the direct values-string slow-path continuation, narrow shared setting/motion, classic display-coordinate, I/O, DS, JSON, value, instance, layer, collision, platform, draw, legacy-action, and input operations that still have cross-file callers, the one opaque shared graphics-context alias and its static-inline renderer-context accessor, plus header-local numeric-argument and little-endian readers | `gml_builtin.h`, public renderer and software-3D value types needed by crossing declarations, and standard fixed-width/allocation declarations | Family implementation bodies, exact-name metadata, VM internals, renderer or software-3D internals/storage, public consumers, a second builtin-state layout or object, additional shared graphics aliases, or indirect dispatch | Family owners touch only their resource fields; cross-file declarations retain one implementation and are removed when the final external caller disappears; header helpers are pure `static inline` leaves | Declares storage only; the state owner supplies lifecycle and the existing staged VM payload | `make architecture-check`; `make check TEST=builtin_state` |
| `src/runtime/builtins/gml_builtin_state.c` | Sole creation, lazy allocation, reset, destruction, defaults, owned-value traversal/release, resource profiling, and staged canonical codec for all builtin-owned resources | Private builtin storage, public value teardown, I/O file-close leaf, mixer sound and Wwise-bank identity/rehydration operations, and the opaque VM-state cursor | Family algorithms, exact-name dispatch, VM representation or field order, root framing, an independent state header/schema/transaction, or ambient host operations | Owns the one engine-associated `GmlBuiltinState`; coordinates INI, file/buffer, async, DS, time-source, spatial/external-audio/Saudio/Wwise state, physics, and dispatcher resource lifetime | Writes and validates bounded dynamic handles, logical paths, SHA-256 identities, Saudio IDs, and Wwise bank identities in the established audio stage; transient handles, buffers, queues, and caches are excluded | `make check TEST=builtin_state`; `make check TEST=state_security`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_animation.c` | Animation-curve chunk lookup, skeleton instance-value helpers and language-visible skeleton adaptation, plus the ordered animation, GC, shader-fallback, and platform-service fallback stage | Builtin-private ordered-stage and shared DS-lookup declarations, normalized VM/content declarations, host diagnostics, renderer type declaration, and standard math/formatting/allocation/string operations | DS container ownership, instance lifetime, physics implementation, registry ownership, renderer internals, mutable process-global state, or alternate dispatch precedence | Delegates unrecognized names directly to `gml_builtin_try_physics`; skeleton values remain in VM-owned instances and DS string lookup remains one facade implementation | No state section; VM serialization retains instance values | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_physics.c` | Physics fixture/joint algorithms, room-scale and instance-mass lookup, and ordered physics dispatch | Builtin-private resource fields and ordered-stage declarations, normalized VM/content declarations, host diagnostics, renderer type declaration, standard math/string operations | Resource lifecycle or canonical codec ownership, physics simulation ownership, platform-extension implementation, VM scheduling, renderer internals, process-global state, or alternate dispatch precedence | Delegates unrecognized names directly to `gml_builtin_try_platform_extensions`; fixture and joint pools live in the one builtin state | The builtin-state owner retains the existing physics stage and bytes | `make check TEST=builtin_dispatch`; `make check TEST=builtin_state` |
| `src/runtime/builtins/gml_builtin_actions.c` | Early portable legacy adapters plus the later drag-and-drop action continuation and its local drawing helpers | Builtin-private shared action operations and ordered-stage declarations, normalized VM instance/room/RNG operations, compatibility policy, typed renderer and particle operations, and standard math/formatting/string operations | Exact-ID registry ownership, VM event/lifetime implementation, renderer or particle internals, duplicate action helpers, process-global state, or alternate dispatch precedence | The early stage delegates to `gml_builtin_try_instances`; the later stage delegates to `gml_builtin_try_instances_queries`; helpers with exact-ID callers remain single facade implementations | No independent state framing | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_input.c` | Keyboard-map, gamepad, pointer, and joystick argument adaptation plus ordered input dispatch | Builtin-private shared input helpers, coarse classic display-coordinate policy, ordered-stage declarations, normalized VM input operations, renderer type declaration, and standard string comparison | Host adapter input polling, engine input storage, presentation-size policy, I/O implementation, renderer internals, duplicate input helpers, process-global state, or alternate dispatch precedence | Delegates unrecognized names directly to `gml_builtin_try_io`; shared keyboard/gamepad and mouse-button helpers remain single implementations used by exact and ordered dispatch | No independent state framing | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_io.c` | Wildcard and content-relative path handling, VFS-backed text/binary file adaptation, INI parsing, buffer adaptation, MD5/SHA-1/base64 helpers, asynchronous save/load request queuing, host-file close, and both ordered I/O stages | Builtin-private resource fields and exact-ID/ordered-stage declarations, normalized VM operations, injected host/VFS services, and standard character/integer/formatting/allocation/string operations | Resource lifecycle or canonical codec ownership, ambient OS I/O, host/VFS implementation, DS/JSON conversion, exact-name registry ownership, process-global state, or alternate dispatch precedence | The INI stage delegates directly to `gml_builtin_try_particles` and the primary I/O stage to `gml_builtin_try_instances_scripts`; file handles, INI values, buffers, and async queues live in the one builtin state; the state owner invokes the I/O-owned close leaf | The builtin-state owner retains existing INI bytes; file handles, buffers, and async queues remain transient | `make check TEST=builtin_dispatch`; `make check TEST=builtin_state` |
| `src/runtime/builtins/gml_builtin_ds.c` | Map, list, grid, and priority-queue builtin algorithms, lifecycle operations, text serialization, exact-ID direct operations, bounded item views, and ordered DS dispatch | Builtin-private resource fields and stage declarations, JSON writer/decoder, value comparison/sorting, formatting, RNG, read-path and VM declarations; injected host/VFS services; standard math/allocation/string operations | Resource lifecycle or canonical codec ownership, JSON parser/encoder implementation, exact-ID registry ownership, VM state framing, map/list access outside typed operations or views, duplicate container algorithms, process-global state, or alternate dispatch precedence | Receives platform misses and delegates DS misses directly to `gml_builtin_try_json`; exact-ID dispatch uses named operations and JSON uses bounded views/ID mutation while all pools live in the one builtin state | The builtin-state owner retains all existing DS positions and bytes | `make check TEST=builtin_dispatch`; `make check TEST=ds_grid`; `make check TEST=builtin_state` |
| `src/runtime/builtins/gml_builtin_json.c` | The one JSON parser, encoder, streaming writer/decoder bridge, and ordered JSON dispatch | Builtin-private DS value-copy views and ID-based mutation, value-storage cloning, ordered-stage declarations, normalized VM struct/value operations, host diagnostics, and standard character/math/integer/formatting/allocation/string operations | DS container storage or hashing, DS text-envelope policy, VM state framing, duplicate parser/encoder implementations, exact-name registry ownership, process-global state, or alternate dispatch precedence | Receives DS misses and delegates JSON misses directly to `gml_builtin_try_values_variables`; DS serialization reuses the same writer/decoder, and JSON never inspects map/list arrays | No independent state framing; decoded DS values live in builtin state while structs and arrays remain VM-owned | `make check TEST=d3_state`; `make check TEST=persistent_room`; paired DS dispatch characterization |
| `src/runtime/builtins/gml_builtin_audio.c` | Audio argument adaptation, spatial-emitter policy, portable Saudio/SGAudio/SuperSound/BGM/GMFMODSimple/FAudioGMS/pxwrap/Wwise adapters, sidecar and bank loading, hash-checked state rehydration, and ordered audio dispatch | Builtin-private audio/String-ID operations, ordered-stage declarations, typed mixer/codec/bank operations, host/VFS services, and standard math/string operations | Generic encoded-call decoding, dynamic-library loading, microphone fabrication, game identities or hardcoded hashes, mixer implementation, content routing, VM scheduling, process-global state, or alternate dispatch precedence | Maps recognized external audio APIs to the mixer; unknown targets remain unsupported; known playback APIs need no Windows DLL | The builtin-state owner retains bounded dynamic handles, paths, hashes, Saudio IDs/positions, and Wwise bank metadata; mixer state retains sounds and voices | `make check TEST=builtin_dispatch`; `make check TEST=builtin_state`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_instances.c` | Instance creation/destruction, activation, queries, collision-facing lookup, movement and alarms, room flow and view overrides, script/event invocation, path/timeline adapters, time-source algorithms, and their ordered continuations | Builtin-private shared array/time-source/room-order/script-reference, collision-query, motion, logging, and ordered-stage operations; normalized VM instance/room/path/timeline APIs; compatibility policy; host diagnostics; typed renderer declarations; and standard numeric/string operations | Time-source lifecycle or canonical codec ownership, VM scheduling, instance/collision/room lifetime implementation, path/timeline stepping, registry ownership, renderer internals, duplicate shared helpers, process-global state, or alternate dispatch precedence | Values delegate to instance destruction, then through legacy actions back to instance queries before particles and draw; the remaining instance chain ends at `gml_builtin_try_layers_early`; time-source storage lives in the one builtin state while instance, room, path, and timeline storage remains VM-owned | The builtin-state owner retains existing time-source positions and bytes; VM serializer retains instance/room/path/timeline fields | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room`; `make check TEST=builtin_state` |
| `src/runtime/builtins/gml_builtin_particles.c` | Particle type, system, particle, and emitter builtin argument adaptation plus its ordered stage | Builtin-private argument and stage declarations, normalized VM layer access, typed particle and renderer operations, and standard fixed-width/string operations | Particle simulation, procedural masks, particle pools or state serialization, renderer internals, registry ownership, process-global state, or alternate dispatch precedence | Receives misses from instance queries and delegates unrecognized names directly to `gml_builtin_try_draw`; all particle storage remains owned by the one VM-associated particle subsystem | No independent state framing; `src/runtime/particles/` retains the canonical particle payload | `make check TEST=builtin_dispatch`; `make check TEST=d3_state` |
| `src/runtime/builtins/gml_builtin_draw.c` | Draw, camera, vertex, model, matrix, shader-texture, font/surface, and fixed-function builtin argument adaptation at both ordered draw stages | Builtin-private shared operations and ordered-stage declarations, normalized VM value/content operations, compatibility policy, host diagnostics, and typed renderer/software-3D operations and value copies | Renderer or software-3D storage, rasterization, resource lifetime, canonical state, registry ownership, process-global state, or alternate dispatch precedence | Receives particle misses and delegates drawing misses directly to input; the later D3 stage receives variable-stage misses and delegates directly to animation; all video resources and fixed-function state remain video-owned | No independent state framing; VM, renderer, and software-3D state owners retain the existing canonical bytes | `make check TEST=builtin_dispatch`; `make check TEST=d3_state`; draw characterization |
| `src/runtime/builtins/gml_builtin_registry.h` | Sole canonical rows for every exact spelling, stable append-only builtin ID, alias, family owner, dispatch stage, cache policy, and explicit dynamic-prefix ID | Compile-time enum and X-macro declarations only | Implementation bodies, lookup slots, VM state, duplicate metadata, implicit ID renumbering, or runtime mutation | None; all data is immutable compile-time metadata | No state section; IDs are internal execution identities, not serialized state | `make builtin-registry-check` |
| `src/runtime/builtins/gml_builtin_registry.c` | Canonical metadata expansion, immutable hash-index lookup, cache-policy application, direct cached-ID execution, and direct routing of other cacheable IDs to their declared family stage | Canonical registry rows, generated data-only index, builtin-private shared facade/family operations, normalized VM operations, host diagnostics, and typed AV operations | Family implementation bodies, VM or renderer storage, process-global state, duplicate exact-name tables, callbacks or function pointers in cached dispatch, or alternate precedence | Resolves exact names without allocation; contextual and after-script rows deliberately decline caching; all subsystem state remains with its existing owner | No independent state framing; IDs and lookup slots are derived and are not serialized | `make builtin-registry-check`; `make check TEST=builtin_dispatch`; paired registry characterization |
| `src/runtime/builtins/gml_builtin_layers.c` | Exact-name layer adaptation, early tile-layer mutation, and runtime layer, element, and tilemap adaptation at both established ordered positions | Builtin-private shared layer/result operations and ordered-stage declarations, normalized VM layer/tilemap operations, host diagnostics, renderer type declaration, and standard numeric/string operations | VM room/layer lifetime, draw traversal, platform implementation, registry ownership, renderer internals, duplicate shared helpers, process-global state, or alternate dispatch precedence | Early and late stages delegate directly to `gml_builtin_try_platform` and `gml_builtin_try_platform_tail`; the platform-extension stage calls the exact adapter directly; runtime layer and tilemap storage remains VM-owned | No independent state framing; VM serializer retains the existing fields | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_platform.c` | Deterministic platform and offline-capability adaptation, generic encoded external-call decoding/dispatch, host-backed wall time and date formatting, bounded classic assignment execution, network capability policy, dialog and extension fallbacks, explicit host/software-renderer noops, and all three established platform positions | Builtin-private ordered stages and external adapter declarations, shared platform/renderer operations, animation-owned skeleton and layer-owned exact adapters, compatibility policies, injected host services, public renderer operations, normalized VM operations, and standard character/math/integer/formatting/allocation/string operations | Ambient OS or network access, native dynamic-library loading, Valve/Steam integration, skeleton/layer/DS/JSON implementation, renderer internals, VM scheduling, registry ownership, process-global state, or alternate dispatch precedence | Self-describing aliases dispatch by library/symbol name; Steam-family libraries resolve to deterministic offline no-ops; the primary stage delegates directly to `gml_builtin_try_ds` | No independent state framing; builtin buffers remain transient, while window, GUI, and classic-information fields remain covered by the canonical VM serializer | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room`; `make check TEST=d3_state` |
| `src/runtime/builtins/gml_builtin_collision.c` | Collision query geometry, candidate-grid construction and collection, contact resolution, shape/line/list queries, motion/path/grid planning, nearest/furthest selection, and ordered collision adaptation | Builtin-private shared policy/container/motion/state operations and the direct values-string slow-path continuation, normalized VM instance/path storage, typed renderer collision metadata, compatibility accessors, host diagnostics, and standard math/allocation | Instance lifetime, collision-event scheduling, value/string implementation, renderer internals, process-global state, or alternate dispatch precedence | The collision stage delegates directly to `gml_builtin_try_values_math`; planning misses use the direct facade continuation; cached grid-mode policy and planning grids remain in the one shared private builtin state and candidate arrays stay VM-owned derived scratch | No independent state framing; candidate and planning scratch remain transient and canonical VM state bytes are unchanged | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room` |
| `src/runtime/builtins/gml_builtin_values.c` | Ordered math, color, pure geometry, array, string, variable, struct, method binding/construction, method-classification, and recursive value-cloning adaptation, including local point-in-polygon, string-iteration, Unicode, public-struct-name, and clone-graph helpers | Builtin-private crossing helpers and ordered stages, normalized VM value/array/RNG/callable/struct operations, compatibility accessors, injected host randomness, renderer type declaration, collision operations, and standard character/math/allocation/formatting/string operations | DS container or JSON conversion ownership, instance destruction/query ownership, legacy actions, platform capability policy, collision implementation, VM value representation, renderer internals, process-global state, or alternate dispatch precedence | The value-math stage delegates to `gml_builtin_try_collision_planning`, the string stage to `gml_builtin_try_io_ini`, the language stage to `gml_builtin_try_instances_destroy`, and the variable stage to `gml_builtin_try_draw_3d`; exclusive geometry, string, struct-name, and clone-graph helpers remain local, the single value-storage clone is privately shared with JSON, and other shared helpers retain declarations only while they have cross-file callers | No independent state framing; value graphs, arrays, variables, structs, and RNG remain in the canonical VM payload and builtin policy remains in the shared transient state | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room`; paired value-dispatch characterization |
| `ANYGM_VM_SOURCES` | One language-value implementation followed by bytecode execution, frame, instance/event/collision, lifecycle/coordination, room/path/timeline, RNG, canonical-state owners, and an optional compile-time fine-trace owner | Public and VM-family-private value boundaries, normalized content, builtin operations, renderer/audio operations, compatibility policy, and host services | Builtin-resource layout or field access, adapter APIs, renderer-private storage, alternate value models, duplicated value algorithms, or a second random stream | One caller-owned value graph, instance pool, room/path set, random stream, opaque builtin-state pointer, and diagnostic-build-only trace pointer inside each engine-owned `GmlVM`; no process-global mutable state | Value graphs, instances, room/path state, RNG fields, and delegated builtin stages remain serialized in canonical order by the VM state owner; diagnostics are excluded | `make check TEST=persistent_room`; `make check TEST=builtin_state`; `make diagnostics-check` |
| `src/runtime/vm/gml_value.h` | Sole definitions of language values, arrays, variable maps, inline constructors, public operations, and bounded alias-deduplicating release | Fixed-width and size C types only | Complete VM state, instances, renderer, audio, host services, or format policy | Declares caller-owned value graphs and maps; owns no process-global state | Value lifetime is serialized by the owning state section | `make check TEST=persistent_room`; `make check TEST=builtin_state` |
| `src/runtime/vm/gml_value.c` | Sole implementation of value-name hashing, variable maps, arrays, string/array clone semantics, escape marking, bounded release, and deduplicated value-graph teardown | `gml_value_internal.h` and standard allocation/string primitives | Complete VM execution, state framing, renderer/audio operations, compatibility policy, host services, adapters, or an alternate value model | Mutates only caller-owned values, maps, arrays, and teardown contexts | Owns representation and lifetime algorithms; canonical serialization remains with state owners | `make check TEST=persistent_room`; `make check TEST=builtin_state` |
| `src/runtime/vm/gml_value_internal.h` | VM-family-private hashed map access, array representation maintenance, and deduplicated teardown contract | `gml_value.h` and standard size/fixed-width types | Builtins, core, renderer, audio, adapters, host services, or format policy | Declares one caller-owned teardown context; no global mutable state | Value graphs remain part of the canonical VM payload | `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm.h` | Subsystem-facing VM records and coarse execution, instance, room, input, RNG, and state operations | Narrow value, normalized content, and software-3D value types | Concrete builtin-resource types or fields, renderer-private records, adapter types, or implementation bodies | Declares the one engine-owned `GmlVM` composition with one opaque `GmlBuiltinState *` | Declares the canonical VM payload operations | `make check TEST=persistent_room`; `make check TEST=builtin_state` |
| `src/runtime/vm/gml_vm_internal.h` | VM-family-private direct byte/value readers, hashed variable and instance operations, motion synchronization, code-cache preparation, opaque instance lookup/bounds, instance/event phase operations, room layer operations, frame cleanup, restore helpers, and the sole struct-slot ID layout shared by execution, lifecycle, room, frame, and canonical-state owners | `gml_vm.h` and fixed-width C types | Builtins, core, renderer, audio, adapters, host services, alternate VM storage, exposed pool representation outside direct owners, callbacks for direct hot paths, or generic field getters | Declares no storage or mutable global state | State-owner dependencies and struct reconstruction remain explicit while canonical bytes stay with the one state implementation | `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm_diagnostics.c` | Diagnostic-build-only strict selector parsing, bounded JSON record emission, and filtered opcode, event, collision, and variable-write observation | VM-family-private state, normalized code/object/instance metadata, values, and host diagnostics | Release builds, public or adapter APIs, filesystem output, canonical state, alternate dispatch, callbacks in hot paths, content identity policy, or unbounded output | Owns one optional trace record allocated inside its caller-owned `GmlVM`; direct hook macros disappear when `ANYGM_DIAGNOSTICS` is absent | Entirely transient and excluded from every canonical state section | `make diagnostics-check` |
| `src/runtime/vm/gml_vm_exec.c` | Sole bytecode dispatch, variable/scope resolution including bounded late classic global declarations, instance-field access, code-cache analysis, microcode recognition, and callable-invocation owner | Public and VM-family-private value operations, normalized code, builtins, typed renderer/input operations, compatibility policy, and host services | Object/room lifecycle coordination, canonical-state framing, adapters, alternate interpreters, callback-based opcode dispatch, or process-global mutable state | Mutates execution fields and value graphs inside one caller-owned `GmlVM`; caches stay content-owned | Existing execution and value fields remain serialized by `gml_vm_state.c`; code caches remain derived | `make check TEST=builtin_dispatch`; `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm_frame.c` | Sole step and draw phase-ordering, frame-snapshot, asynchronous input/event-drain, visual layer-filter, read-only tile-projection, and frame-scratch owner | Public values, VM-family-private instance/room/value operations, normalized content, typed renderer/audio/particle operations, compatibility policy, and host services | VM lifecycle, room mutation policy, object/instance-pool representation, bytecode dispatch bodies, canonical-state framing, adapters, alternate frame engines, callbacks in hot loops, or process-global mutable state | Coordinates frame-visible fields and reusable draw scratch inside one caller-owned `GmlVM` | Mutates established VM fields only; canonical ordering and framing remain with `gml_vm_state.c` | `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm_instances.c` | Sole object parsing, instance lifetime, event lookup/dispatch, collision-event pair geometry/dispatch, and room/view boundary owner | Public values, VM-family-private operations, normalized content, typed renderer metadata, compatibility policy, and host diagnostics | Language-visible collision query adapters/indexes, frame/draw coordination, room-entry policy, bytecode dispatch bodies, canonical-state framing, adapters, alternate instance engines, or callbacks in hot paths | Mutates the instance pool, object caches, and event scratch inside one caller-owned `GmlVM` | Existing object, instance, and derived-cache fields remain serialized or rebuilt by the canonical state owner | `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm.c` | VM lifecycle, subsystem wiring, input services, shared comparison and byte utilities, and coarse cheat control | Public values, VM-family-private cleanup/initialization operations, normalized content, typed subsystem operations, compatibility policy, and host services | Frame/draw scheduling, bytecode, object/instance/event/collision bodies, room lifecycle or mutation-policy bodies, adapter APIs, renderer/audio private storage, canonical-state framing, alternate engines, or process-global mutable state | Creates, initializes, and destroys one caller-owned `GmlVM` and reached subsystem contexts | Coordinates the canonical VM state owner; does not own state framing | `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm_rooms.c` | Sole room lookup/entry, persistent-room state, runtime-layer/tilemap, room asset-warming, path/timeline resource and stepping, and tile-layer mutation-state owner | VM-family-private byte, value, instance, and code operations; normalized content; typed renderer metadata and audio warming; compatibility policy; and host diagnostics | Interpreter dispatch, object/instance-pool representation, frame/draw scheduling or read-only draw projection, canonical-state framing, adapter APIs, alternate room engines, or process-global mutable state | Mutates room, layer, tilemap, path, timeline, and active-instance fields inside one caller-owned `GmlVM` | Existing room, layer, tilemap, path, timeline, and instance fields remain serialized in canonical order by `gml_vm_state.c` | `make check TEST=persistent_room` |
| `src/runtime/vm/gml_vm_rng.c` | Sole implementation of compatibility-aware seed expansion, random-stream advancement, and optional RNG diagnostics | Subsystem-facing VM storage, named compatibility policy, and host diagnostics | Builtin dispatch, state framing, alternate generators, ambient randomness, adapters, or process-global mutable state | Mutates only the one random stream embedded in its caller-owned `GmlVM` | RNG fields remain serialized in canonical order by `gml_vm_state.c` | `make check TEST=rng` |
| `src/runtime/vm/gml_vm_state_codec.h` | Opaque bounded reader/writer cursor operations used to delegate storage-owned resource stages without exposing VM state internals | Narrow values and incomplete `GmlVM` declaration | Inclusion outside `gml_vm_state.c` and `gml_builtin_state.c`, cursor representation, root framing, schema ownership, or implementation bodies | Declares no storage | Adds no section or schema; preserves caller-owned field positions | `make architecture-check`; `make check TEST=builtin_state` |
| `src/runtime/vm/gml_vm_state.c` | Sole canonical VM payload size/save/load implementation, value-graph framing, deterministic ordering scratch, opaque cursor implementation, restore-time cleanup, and transactional reconstruction | VM-family-private value and operation boundaries plus coarse builtin-state stage, renderer, audio, particle, and host operations | Builtin-resource layout, root framing, adapters, alternate schemas, duplicate VM storage, or execution algorithms unrelated to restore | Reads and restores one caller-owned `GmlVM`; owns only state-operation scratch | VM schema 4 payload inside the current root schema; array representation metadata, field order, and rejection remain canonical while four resource positions delegate to the builtin-state owner | `make check TEST=state_security`; `make check TEST=builtin_state` |
| `tests/unit/runtime/test_vm_diagnostics.c` | Synthetic strict-filter, output-bound, JSON-escaping, and malformed-configuration coverage for optional VM tracing | Diagnostic VM boundary, synthetic object/code/instance metadata, host setting and log callbacks, and the minimal named test runner | External content, filesystem evidence, release-core linkage, or mutable cross-case state | One clean stack fixture and trace allocation per named case | Test-only; asserts that diagnostics remain outside canonical state | `make diagnostics-check` |
| `ANYGM_RENDERER_SOURCES` | Renderer facade, complete surface ownership and composition, atlas decode/prefetch, font/text layout and drawing, recognized-shader controls, non-CRT layer effects, recognized display post-processing, cohesive software blit, fixed-function primitive kernels, and canonical renderer payload serialization | Host, compatibility, codecs, software 3D | Builtin implementation, VM internals, core internals, or adapter APIs | One `GmlRender` and its caches | Renderer payload owner; root framing remains in core | `make check TEST=d3_state` |
| `src/video/renderer/gml_render.h` | Opaque renderer operation declarations, value-copy draw/target/presentation/shader/texture results, the bounded application-surface write view, and public layer-filter policy values | Narrow public and subsystem value types | Private record definitions, storage layout, borrowed private records, or stateful inline bodies | None | Declares coarse masked state, clear and surface-binding operations, renderer-owned texture resolution with value-copy metrics, and opaque shader controls; callers cannot dereference storage | `make architecture-check` |
| `src/video/renderer/gml_render_internal.h` | Private renderer asset/cache records and complete storage composition | Public renderer policy declarations and owned subsystem context types | Implementation bodies or adapter APIs | Sole definitions of `struct GmlRender` and its private record representations | Renderer `.c` owners include it explicitly; `engine_internal.h` uses it only for by-value size; canonical bytes remain with the state owner | `make architecture-check` |
| `src/video/renderer/gml_render_pixel_internal.h` | Header-local color-write masking and fixed-function multiply blending shared by complete surface pixel kernels | Renderer-private storage only | Public or cross-subsystem inclusion, mutable storage, allocation, indirect dispatch, or family-specific sampling policy | None | No state section; definitions remain `static inline` in each renderer owner that consumes them | `make check TEST=renderer_postprocess` |
| `src/video/renderer/gml_render_sampling_internal.h` | Canonical header-local recognized-shader activation, alpha-discard, palette, LUT, grid, displacement, grayscale, and mapped-texture sampling operations shared by complete renderer kernels | Renderer-private asset/storage reads and atlas warming only | Public or cross-subsystem inclusion, allocation, mutable state ownership, indirect per-pixel dispatch, or arbitrary shader interpretation | None; operations read one caller-owned `GmlRender` | No state section; definitions remain `static inline` in each complete kernel owner that consumes them | `make check TEST=d3_state` |
| `src/video/renderer/gml_render_blit_internal.h` | Canonical header-local cardinal-anchor, opacity-run, fast-alpha, and interpolated row-band helpers shared by complete sprite and surface composition kernels | Renderer-private storage, recognized-shader sampling, math, and architecture-specific vector intrinsics | Public or cross-subsystem inclusion, mutable storage ownership, allocation outside established callers, indirect per-pixel dispatch, or duplicated helper bodies | None; helper records and operations remain local to each consuming renderer translation unit | No state section; definitions remain direct and header-local | `make check TEST=renderer_surfaces` |
| `src/video/renderer/gml_render_backend.h` | Non-reentrant pixel and stretched-surface services plus borrowed representation-neutral texture and current draw-target views | Opaque renderer context, primitive value types, and explicit leaf state operations | Renderer-private record types or high-level draw dispatch | Views borrow renderer-owned pixel planes only until the next renderer operation | None | `make architecture-check` |
| `src/video/renderer/gml_render.c` | Renderer lifecycle, frame and target coordination, application-surface binding/mirroring, deferred-draw preparation, and coarse control, resource, diagnostic, draw-state, presentation, sample-plane, shader, GPU, and target-view backend operations | Public renderer facade, renderer implementation owners, and video-private storage | Core or builtin policy resolution, VM scheduling, adapters, asset lifecycle, complete raster or surface composition bodies, or per-field external accessors | Mutates one `GmlRender`; returns value-copy metrics and accepts explicit bounded pixel-plane or composition borrows | No canonical payload ownership | `make check TEST=d3_state` |
| `src/video/renderer/gml_render_assets.c` | Value-copy sprite/background/font/tile and generic texture queries, complete runtime static/animated sprite and background image replacement lifecycle, collision-mask/deletion lifecycle, narrow sprite playback mutation, opaque texture-handle creation, and tagged handle resolution into neutral backend views | Renderer-owned asset records, image codec and VFS reads, established cache cleanup, warm/decode operations, and host diagnostics | Raster kernels, builtin implementation, adapters, VM policy, or software-3D state | Backend views borrow renderer-owned pixels; public metadata snapshots copy dimensions and tags and borrow only optional resource names | Runtime sprite records use the canonical renderer payload; runtime background and decoded/cache pixels remain disposable | `make check TEST=image_codec`; `make check TEST=renderer_tiles`; `make check TEST=d3_state` |
| `src/video/renderer/gml_render_blit.c` | Complete sprite, background, room-tile, paint, axis-cache, shader-fill, and low-level pixel raster closure, including fractional single-texel part extents | Renderer-private storage, header-local sampling/blending helpers, coarse draw preparation, atlas warming, row-band execution, compatibility policy, and host diagnostics | Asset record lifecycle, surface-pool ownership, VM/builtin policy, adapters, state framing, or non-raster feature policy | Mutates only active target pixels plus renderer-owned disposable raster caches; its cohesive mutually calling hot closure is the documented exception to the soft source-size trigger | Raster caches are disposable; asset and shader policy state remains with their canonical renderer owners | `make check TEST=d3_state D3_TEST_ARGS='--case raster.primitives_and_models'`; `make check TEST=renderer_effects` |
| `src/video/renderer/gml_render_atlas.c` | Texture-blob decode, atlas cache accounting, worker-pool lifecycle, prefetch/warm operations, and TXTR record construction | Renderer-private byte readers and settings, host diagnostics, thread leaves, image/QOI/BZip2 codecs | Draw dispatch, VM policy, adapters, surfaces, or canonical state framing | Mutates atlas records and the instance-owned prefetch pool; synchronous draw fallback remains timing-independent | None; decoded pixels are disposable caches | `make check TEST=d3_state` |
| `src/video/renderer/gml_render_postprocess.c` | The recognized display post-process kept in software: the two-sample channel-offset family, with its sampler, row-band context, fast/general paths and pixel kernel | Renderer-private surface/asset reads, final-pixel inline operations, coarse opacity and draw preparation, settings/diagnostics, CPU count, and row-band execution | Shader parsing, arbitrary interpretation, surface-pool ownership, VM/builtin policy, adapters, per-pixel indirect dispatch, or any kernel that reproduces a content shader's own expression rather than a parameterised operation | Mutates one active target owned by one `GmlRender` | No caches; recognized shader parameters remain in the canonical renderer payload | `make check TEST=renderer_postprocess` |
| `src/video/renderer/gml_render_effects.c` | Room-layer target capture, composition, and nine layer-effect kernels specified in `docs/EFFECT_LAYERS.md`; shader recognition remains incomplete in this historical tree | Renderer-private content reads during initialization, target preparation, asset sampling, camera synchronization, and pass-level row-band execution | Arbitrary shader interpretation, VM or builtin policy, adapters, surface ownership, per-pixel indirect dispatch, or unrelated shader-program interpretation | Initializes renderer-owned shader policy records and mutates one `GmlRender` plus disposable effect workspaces | Recognized policy is canonical renderer state; effect workspaces are disposable caches | `make check TEST=renderer_effects` |
| `src/video/renderer/gml_render_primitives.c` | Fixed-function clear, point, rectangle, line, circle, colour-fan, triangle, and layer-fill pixel kernels, including continuous-geometry circle placement | Renderer draw state and private pixel backend leaves | Builtin argument decoding, VM scheduling, or software-3D recursion | Mutates only the active target or its deferred full-target fill owned by one `GmlRender` | None | `make check TEST=d3_state` |
| `src/video/renderer/gml_render_surfaces.c` | Surface lookup and coverage metadata, allocation, copy, resize, free, target-stack operations, and complete normal, interpolated, stretched, part, and rotated composition kernels; the mid-frame content-program hand-off to the host and the composition of its answer | Public surface operations, renderer-private storage and header-local blit/sampling/pixel helpers, coarse draw preparation, recognized post-process owners, software-3D backend, host diagnostics, compatibility policy, and camera synchronization | Sprite kernels, shader recognition, VM/builtin implementation, adapters, per-pixel cross-unit calls, or canonical state framing | Mutates surface records, application-surface ownership, the bounded target stack, and the active target of one `GmlRender` | Surface pixels and metadata remain in the canonical renderer payload owned by `gml_render_state.c` | `make check TEST=renderer_surfaces` |
| `src/video/renderer/gml_render_text.c` | FONT parsing, built-in and runtime font construction, glyph lookup/layout, text measurement/drawing, and classic information-page rendering | Renderer-private atlas, sprite, draw, host, VFS, and the media font-raster boundary | Builtin argument decoding, VM scheduling, surfaces, effects, raw third-party font APIs, or root-state framing | Mutates font and disposable atlas records owned by one `GmlRender`; draws only to its active target | Renderer font records remain inside the canonical renderer payload | `make check TEST=persistent_room` |
| `src/video/gpu/gml_gpu.c` | Context lifetime, eligibility, fallback accounting, and the one crossing into a backend | The neutral plan, the backend boundary, and standard allocation | Any graphics API, renderer or engine internals, frontend-adapter types, or process-global state | Owns one `GmlGpu` per engine | None; no GPU record is serialized under any schema | `make check TEST=gpu` |
| `src/video/gpu/gml_gpu_gl.c` | The only owner of graphics API types, constants and entry points: instance dispatch, program and vertex array, source mirrors keyed by identity and generation, integer axis maps, explicit state discipline, rectangle orientation, and the content's own programs: the dialect rewrite, a per-context program cache with declared-uniform tables, typed value delivery, and the quad that draws a frame through one | Its own private declaration set and the entry points the host resolved | Platform graphics libraries, ambient symbol lookup, window or device APIs, renderer or engine internals, or frontend-adapter types | Owns one backend record per `GmlGpu` | None | `make check TEST=gpu` |
| `src/video/renderer/gml_render_presentation.c` | The deferred terminal presentation: whether the frame's last operation may be recorded instead of written, the record (including the content program it was drawn through, when one was), and the one kernel that writes it under either sampling convention | Renderer-private storage, the coarse draw-preparation and pending-fill operations, the target-coverage query, and the backend fill leaf | Composition kernels, shader recognition, graphics API types, adapters, or a second copy of either sampling expression | Writes only the buffer the record names | None; a deferred presentation is never serialized | `make check TEST=render_plan` |
| `src/video/renderer/gml_render_plan.c` | Render-plan lifetime, bounded recording, complete structural validation, the exact axis-mapping rules, map composition, and the eligibility facts that decide whether a pass may leave the software executor | The plan's own value records and standard math and string operations | Renderer-private storage, GL or libretro types, host services, allocation on a hot path, or any knowledge of a graphics backend | Owns nothing; a plan is a bounded value record supplied by its caller | None; a plan is never serialized under any schema | `make check TEST=render_plan` |
| `src/video/renderer/gml_render_plan_software.c` | The exact software executor for a plan: nearest magnification by both characterized axis rules, the box reduction, the clear, and complete CPU-frame presentation | Plan value records and standard allocation and memory operations | Renderer-private storage, GL or libretro types, host services, VM or engine state, or an approximate substitute for a characterized kernel | Writes only the caller-owned target it is handed | None | `make check TEST=render_plan` |
| `src/video/renderer/gml_render_state.c` | Canonical renderer payload size/save/load and value-copy profiling metrics | Renderer-private font, draw-state, surface, and runtime-sprite records; host diagnostics | Root headers, VM state, audio state, or root rollback policy | Reads and restores one `GmlRender`; save may refresh owned surface RLE caches | Renderer payload inside the current root schema; no independent framing | `make check TEST=state_security` |
| `tests/unit/video/renderer/test_renderer_tiles.c` | Synthetic modern tileset layout, animation-cadence, source-frame, runtime background replacement, and mirror/flip/rotate raster coverage | Renderer facade plus private synthetic fixture records | External content, proprietary pixels, VM scheduling, or mutable cross-case state | One isolated renderer fixture per assertion group | Test-only; no persistent state | `make check TEST=renderer_tiles` |
| `ANYGM_SOFTWARE3D_SOURCES` | Fixed-function context, model resources, rasterization, and state | Renderer backend leaf services | Builtin implementation or high-level renderer recursion | One engine-owned `GmlSoftware3D` | Software-3D payload owner | `make check TEST=d3_state` |
| `ANYGM_SOFTWARE3D_TEST_SOURCES` | One named software-3D test binary divided by boundary, asset/surface, language, blend, raster/model, and state owner | Runtime owners under test plus `anygm_test_runner.c` | Production linkage, external corpora, hidden fixture macros, or case-order state | One clean `Software3dRasterFixture` per selected replay | Test-only; canonical state assertions target the production owner | `make check TEST=d3_state` |
| `ANYGM_PERSISTENT_TEST_SOURCES` | One named persistent-runtime test binary divided by VM, sequence, DS, I/O, audio, timeline, state, and renderer owner | Runtime owners under test plus `anygm_test_runner.c` | Production linkage, external corpora, generic aggregate cases, or mutable state shared between registered cases | Per-case synthetic fixture | Test-only; state cases target current canonical VM bytes | `make check TEST=persistent_room` |
| `src/video/software3d/gml_software3d.c` | Context lifecycle plus opaque status, masked control, light, vertex-format, and vertex-buffer operations | Software-3D private storage, model-resource cleanup, and matrix initialization operations | VM ownership, language adapters, renderer storage, or canonical state mapping | Allocates, resets, and destroys one caller-owned `GmlSoftware3D`; returns only bounded value results | None directly | `make check TEST=d3_state` |
| `src/video/software3d/gml_software3d_raster.c` | Matrix value copies and stack operations, borrowed target and texture views, fixed-function projection, clipping, sampling, raster emission, typed 2D/3D primitive bridges, and vertex submission | Public software-3D operations, private raster storage, and non-reentrant renderer backend leaves | Renderer fields, builtin implementation, or high-level renderer recursion | Mutates the caller's opaque `GmlSoftware3D` context; keeps borrowed draw views outside per-pixel loops | Software-3D payload through its owner | `make check TEST=d3_state` |
| `src/video/software3d/gml_software3d_models.c` | Opaque model lifecycle, bounded batch/vertex value-copy inspection, fixed-function model construction/drawing, and canonical model-state serialization | Software-3D private model storage, typed raster operations, and renderer backend leaves | Builtin implementation, VM scheduling, borrowed model storage, or root-state ordering | Mutates model resources in the caller's `GmlSoftware3D` context without exposing their allocation | Canonical model subpayload | `make check TEST=d3_state` |
| `src/video/software3d/gml_software3d_state.c` | Canonical fixed-function state mapping to and from the VM serializer's bounded arrays | Context reset and matrix initialization operations | VM scheduling, root-state framing, or model payload bytes | Restores one caller-owned `GmlSoftware3D` transaction input | Canonical fixed-function subpayload | `make check TEST=d3_state` |

## Build-time audio data

`tools/audiogen/` owns the optional, standalone reproduction of
`src/generated/audio_setup_data.h` and `src/generated/wwise_codebooks.h` from
externally verified BSD Xiph sources. `emit_setups.c` owns reference setup selection and
header emission; `emit_books.c` owns named source-book export; `packed_books.py`
owns compact wire packing; `generate.py` owns verified acquisition, serial builds
and whole-output checking. `recipes.json` contains interoperability parameters and
source-symbol mappings, not copied codebook vectors. None of these build-time
owners is linked into the core or changes the runtime audio-bank boundary.

`make audio-data-check` checks both catalogs' structure and lookup metadata
offline and runs negative drift controls. Complete byte identity requires an
external verification manifest passed to `check_audio_data.py`, or independent
regeneration and comparison; the default structural check cannot establish it.
Full regeneration, including optional ASan/UBSan, is
documented in `tools/audiogen/README.md`. `make provenance-check notices-check`
checks the declared first-party provenance directives and embedded-notice
table consistency; it does not authenticate an external source archive.

## Common change routes

### Add or correct a serialized input field

Reader under `src/content/` -> shared content model -> parser fixture. Add a
compatibility policy only if the field changes runtime semantics.

### Correct a generation-dependent semantic

`anygm_compatibility.c` -> named policy accessor -> smallest branch in the
shared owner -> compatibility unit test plus the owner's focused test.

### Add a platform facility

`src/api/anygm.h` host service -> portable helper in `src/host/` -> adapter
implementation -> dummy-host contract. Do not call the platform directly from
the consumer.

### Change save-state data

Owning subsystem serializer -> root state framing in `engine_state.c` -> transactional
load test -> two-instance integration test -> review stable size bound and
schema compatibility. `anygm_state_size` remains the exact canonical size; any
fixed-capacity transport fallback belongs to an adapter.

### Add another frontend framework

New adapter directory -> `src/api/anygm.h` only -> framework-specific build
target. Follow `PORTING.md`; no VM, renderer, mixer, or parser fork is
needed.
